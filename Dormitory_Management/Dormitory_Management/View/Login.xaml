<Window x:Class="Dormitory_Management.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập hệ thống" Height="450" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Background="#f0f0f0">
        <Border Background="White" CornerRadius="10" Padding="30" Width="400" Height="320" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="ĐĂNG NHẬP" FontSize="22" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Tên đăng nhập" />
                <TextBox x:Name="UsernameBox" Margin="0,5,0,10" Height="30"/>
                <TextBlock Text="Mật khẩu" />
                <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Height="30"/>
                <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="12" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Đăng nhập" Width="100" Click="LoginButton_Click"/>
                    <Button Content="Quên mật khẩu?" Width="120" Margin="10,0,0,0" Click="ForgotPassword_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
