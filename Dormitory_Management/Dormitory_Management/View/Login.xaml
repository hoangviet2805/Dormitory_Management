<Window x:Class="Dormitory_Management.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login System" Height="450" Width="800" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid  Background="Gainsboro">
        <Border Background="White" CornerRadius="10" Padding="30" Width="400" Height="320" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="LOGIN" FontSize="22" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Username" />
                <TextBox x:Name="UsernameBox" Margin="0,5,0,10" Height="30"/>
                <TextBlock Text="Password" />
                <PasswordBox x:Name="PasswordBox" Margin="0,5,0,10" Height="30"/>
                <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="12" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Login" Width="100" Click="LoginButton_Click"/>
                    <Button Content="Forgot Password?" Width="120" Margin="10,0,0,0" Click="ForgotPassword_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
