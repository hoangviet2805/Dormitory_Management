<Page x:Class="Dormitory_Management.View.StudentStatus"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dormitory_Management.View"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="900"
      Title="StudentStatus">

    <Grid Background="#f0f2f5" Margin="10">
        <!-- Phần nhập thông tin tìm kiếm -->
        <Border Background="White" CornerRadius="15" Padding="20" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tiêu đề -->
                <TextBlock Text="Student Status" Grid.Row="0" FontSize="22" FontWeight="Bold" Foreground="#1a73e8" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Thanh tìm kiếm theo số điện thoại -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="Search by Phone Number:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#5f6368" FontWeight="SemiBold"/>
                    <TextBox x:Name="PhoneSearchTextBox" Width="200" Height="35" Margin="0,0,10,0" BorderBrush="#dadce0" BorderThickness="1" Padding="10,5"/>
                    <Border Background="#1a73e8" CornerRadius="5" Width="120" Height="35" Margin="0,0,10,0">
                        <Button Content="Search" Width="120" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="SearchByPhone_Click"/>
                    </Border>
                    <Border Background="#5f6368" CornerRadius="5" Width="100" Height="35" Margin="10,0,0,0">
                        <Button Content="Refresh" Width="100" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="Refresh_Click"/>
                    </Border>
                </StackPanel>

                <!-- Bảng thông tin sinh viên -->
                <DataGrid x:Name="StudentDataGrid" Grid.Row="2" 
                          Margin="0,5,0,0" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"
                          BorderBrush="#dadce0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          CanUserResizeColumns="True"
                          CanUserReorderColumns="True"
                          HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" />
                        <DataGridTextColumn Header="Mobile" Binding="{Binding Mobile}" Width="120" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="120" />
                        <DataGridTextColumn Header="Father's Name" Binding="{Binding Fname}" Width="120" />
                        <DataGridTextColumn Header="Mother's Name" Binding="{Binding Mname}" Width="120" />
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" />
                        <DataGridTextColumn Header="Address" Binding="{Binding Paddress}" Width="120" />
                        <DataGridTextColumn Header="ID Proof" Binding="{Binding Idproof}" Width="120" />
                        <DataGridTextColumn Header="Room No" Binding="{Binding RoomNo}" Width="80" />
                        <DataGridTextColumn Header="Living Status" Binding="{Binding Living}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
