<Page x:Class="Dormitory_Management.View.ManageRoomsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dormitory_Management.View"
      mc:Ignorable="d" 
      Title="ManageRoomsPage" Height="750" Width="1030" >

    <Grid Background="#f0f2f5" Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Column: Add Room -->
        <Border Background="White" CornerRadius="15" Padding="20" Grid.Column="0">
            <StackPanel>
                <!-- Add Room Title -->
                <TextBlock Text="ADD ROOM" FontSize="20" FontWeight="Bold" Foreground="#1a73e8" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <!-- Room Number -->
                <TextBlock Text="Room Number" Foreground="#5f6368" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="RoomNumberTextBox" Margin="0,5,0,15" Height="35" BorderBrush="#dadce0" BorderThickness="1" Padding="10,5"/>

                <!-- Activate Room -->
                <CheckBox x:Name="ActivateRoomCheckBox" Content="Activate" Foreground="#5f6368" FontWeight="SemiBold" Margin="0,5,0,25"/>

                <!-- Add Room Button -->
                <Border Background="#1a73e8" CornerRadius="5" Width="120" Height="35" HorizontalAlignment="Center">
                    <Button Content="Add Room" Width="120" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="AddRoom_Click"/>
                </Border>
            </StackPanel>
        </Border>

        <!-- Right Column: Update or Delete Room -->
        <Border Background="White" CornerRadius="15" Padding="20" Grid.Column="2">
            <StackPanel>
                <!-- Update or Delete Room Title -->
                <TextBlock Text="UPDATE OR DELETE ROOM" FontSize="20" FontWeight="Bold" Foreground="#1a73e8" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <!-- Search Room -->
                <TextBlock Text="Room Number" Foreground="#5f6368" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="SearchRoomNumberTextBox" Margin="0,5,0,15" Height="35" BorderBrush="#dadce0" BorderThickness="1" Padding="10,5"/>

                <Border Background="#1a73e8" CornerRadius="5" Width="120" Height="35" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Button Content="Search" Width="120" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="SearchRoom_Click"/>
                </Border>

                <!-- Activate or Deactivate Room -->
                <CheckBox x:Name="UpdateActivateRoomCheckBox" Content="Activate Room" Foreground="#5f6368" FontWeight="SemiBold" Margin="0,5,0,10"/>
                <CheckBox x:Name="DeactivateRoomCheckBox" Content="Deactivate Room" Foreground="#5f6368" FontWeight="SemiBold" Margin="0,5,0,20"/>

                <!-- Update and Delete Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <Border Background="#28a745" CornerRadius="5" Width="120" Height="35" Margin="0,0,10,0">
                        <Button Content="Update" Width="120" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="UpdateRoom_Click"/>
                    </Border>
                    <Border Background="#dc3545" CornerRadius="5" Width="120" Height="35" Margin="10,0,0,0">
                        <Button Content="Delete" Width="120" Height="35" Background="Transparent" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="DeleteRoom_Click"/>
                    </Border>
                </StackPanel>

                <!-- Room List -->
                <TextBlock Text="ROOM LIST" FontSize="20" FontWeight="Bold" Foreground="#1a73e8" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <DataGrid x:Name="RoomDataGrid" Height="200" AutoGenerateColumns="False" BorderBrush="#dadce0" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNo}" Width="*" />
                        <DataGridTextColumn Header="Active" Binding="{Binding RoomStatus}" Width="*" />
                        <DataGridTextColumn Header="Booked" Binding="{Binding Booked}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
