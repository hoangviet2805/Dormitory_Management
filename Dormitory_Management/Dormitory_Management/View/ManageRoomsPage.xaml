<Page x:Class="Dormitory_Management.View.ManageRoomsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dormitory_Management.View"
      mc:Ignorable="d" 
      Title="ManageRoomsPage" Height="750" Width="1030">

    <Grid Background="#f0f0f0">
        <!-- Thêm Phòng -->
        <Border Background="White" CornerRadius="10" Padding="20" Width="750" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="20">
            <StackPanel>
                <!-- Tiêu đề Thêm Phòng -->
                <TextBlock Text="THÊM PHÒNG" FontSize="18" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Số Phòng -->
                <TextBlock Text="Số Phòng"/>
                <TextBox x:Name="RoomNumberTextBox" Margin="0,5,0,10" Height="30"/>

                <!-- Kích hoạt -->
                <CheckBox x:Name="ActivateRoomCheckBox" Content="Kích hoạt" Margin="0,5,0,10" />

                <!-- Thêm Phòng -->
                <Button Content="Thêm Phòng" Width="120" HorizontalAlignment="Center" Click="AddRoom_Click" Margin="0,10,0,20"/>

                <!-- Cập nhật hoặc Xóa Phòng -->
                <TextBlock Text="CẬP NHẬT HOẶC XÓA PHÒNG" FontSize="18" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Tìm kiếm phòng -->
                <TextBlock Text="Số Phòng"/>
                <TextBox x:Name="SearchRoomNumberTextBox" Margin="0,5,0,10" Height="30"/>

                <Button Content="Tìm Kiếm" Width="120" HorizontalAlignment="Center" Click="SearchRoom_Click" Margin="0,10,0,20"/>

                <!-- Kích hoạt hoặc Hủy kích hoạt -->
                <CheckBox x:Name="UpdateActivateRoomCheckBox" Content="Kích hoạt phòng" Margin="0,5,0,10" />
                <CheckBox x:Name="DeactivateRoomCheckBox" Content="Hủy kích hoạt phòng" Margin="0,5,0,10" />

                <!-- Cập nhật và xóa -->
                <Button Content="Cập nhật" Width="120" HorizontalAlignment="Center" Click="UpdateRoom_Click" Margin="0,10,0,20"/>
                <Button Content="Xóa bỏ" Width="120" HorizontalAlignment="Center" Click="DeleteRoom_Click" Margin="0,10,0,20"/>

                <!-- Bảng Danh Sách Phòng -->
                <TextBlock Text="DANH SÁCH PHÒNG" FontSize="18" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <DataGrid x:Name="RoomDataGrid" Margin="0,5,0,10" Height="150" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số Phòng" Binding="{Binding RoomNo}" Width="*" />
                        <DataGridTextColumn Header="Kích Hoạt" Binding="{Binding RoomStatus}" Width="*" />
                        <DataGridTextColumn Header="Đã Đặt" Binding="{Binding Booked}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
